"""Shared business / data helpers used across multiple Django apps.

* ``algorithms`` — reusable cryptographic / sorting / aggregation helpers.
* ``feature_flags`` — load/merge feature flag JSON in ``SystemSettings``.
* ``utils`` — small generic helpers (``get_client_ip``, etc.).
"""
